# Generated by `dart run mongo_easy:setup` — paste into your
# PowerSync instance (Dashboard → Sync Streams) or deploy with
# the PowerSync CLI. Re-run setup after editing mongo_easy.yaml.
config:
  edition: 3

streams:
  todos:
    auto_subscribe: true
    query: SELECT *, _id as id FROM todos WHERE owner_id = auth.user_id()
